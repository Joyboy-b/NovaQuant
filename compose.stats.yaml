services:
  before-api:
    command: ["uvicorn", "scripts.stats_comparison_app:app", "--host", "0.0.0.0", "--port", "8001"]
    environment:
      OTEL_SERVICE_NAME: novaquant-stats-before
  after-api:
    command: ["uvicorn", "scripts.stats_comparison_app:app", "--host", "0.0.0.0", "--port", "8001"]
    environment:
      OTEL_SERVICE_NAME: novaquant-stats-after
